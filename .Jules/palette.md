## 2026-02-17 - Skip Links in Static Reports
**Learning:** Static HTML reports often miss basic accessibility features like "skip to content" links because they aren't full web applications, but keyboard users still need them to bypass navigation/headers.
**Action:** Always include a skip link and a `<main>` landmark even in simple single-file HTML reports.
