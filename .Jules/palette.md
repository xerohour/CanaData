## 2026-02-23 - Accessible Static Reports
**Learning:** Static HTML reports generated from scripts often lack semantic structure (using `div`s for tables). Adding `th scope="row"` and "Skip to content" links significantly improves accessibility for screen reader and keyboard users without changing the visual design.
**Action:** When generating HTML reports, always use semantic table markup and include bypass blocks (skip links).
